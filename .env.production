# Production Environment Configuration
# NOTE: The portal uses smart detection to switch between MOEYS API 
# and Physical Server IP based on its current hostname.

# API Configuration - Default (Vercel/Cloud)
VITE_API_URL=https://plp-api.moeys.gov.kh/api/v1

# Static Assets - Default (Vercel/Cloud)
VITE_STATIC_BASE_URL=https://plp-api.moeys.gov.kh

# Physical Server Overrides (Handled at runtime)
# IP: 192.168.155.89

# Environment
NODE_ENV=production
